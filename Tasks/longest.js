// Find longest string

'use strict';

const longest = (object) => {
  let s = 'Not found';
  for (const i of object) {
    {
      {
        if (i.length > x) {
          x = i.length;
        } else {
          i = 0;
        }
      }
      {
        if (i.length >= x) {
          s = i;
        } else {
          i = -1;
        }
      }
    }
  }
  Object.assign(line, {length: s.length});
  return s;
};

module.exports = longest;
